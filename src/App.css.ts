import { style } from "@vanilla-extract/css";

export const container = style({
  padding: "3em",
  backgroundColor: "#abc",
});

export const button = style({
  padding: "1em",
  fontSize: "20px",
});
